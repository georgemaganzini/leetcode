var letterCasePermutation = function(s) {
    let res = [];

    const bfs = (string, index) => {
        if (string[index].toLowerCase() != string[index].toUpperCase()) return string;

        //
    }

};